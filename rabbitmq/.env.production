APP_ENV=production
BASE_DOMAIN=nambapp.ru
# Must match RABBITMQ_USER/PASSWORD in app repos (for AMQP).
# Management UI is IP-whitelisted and auto-logs in via Traefik — no browser login.
RABBITMQ_USER=
RABBITMQ_PASSWORD=
