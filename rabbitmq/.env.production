APP_ENV=production
BASE_DOMAIN=nambapp.ru
# Must match RABBITMQ_USER/PASSWORD in app repos (AMQP + management login).
# UI is only protected by Traefik IP whitelist.
RABBITMQ_USER=
RABBITMQ_PASSWORD=
